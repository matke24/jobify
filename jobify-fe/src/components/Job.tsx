import Wrapper from "../assets/wrappers/Job";
import { JobData } from "../types";

const Job: React.FC<JobData> = ({ props }) => {
  return (
    <Wrapper>
      <h2>JOBS</h2>
    </Wrapper>
  );
};

export default Job;
