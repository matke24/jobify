export enum UseNavigationEnum {
  IDLE = "idle",
  SUBMITTING = "submitting",
  LOADING = "loading",
}

export enum SmallSidebarLinks {
  ADD_JOB = "add job",
  STATS = "stats",
  ALL_JOBS = "all jobs",
  PROFILE = "profile",
  ADMIN = "admin",
}
